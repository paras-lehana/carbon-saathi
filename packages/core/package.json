{
  "name": "@carbon-saathi/core",
  "version": "0.2.0",
  "private": true,
  "description": "Pure TypeScript domain engine for Carbon Saathi: emission factors, calculators, schemes, gamification.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
