/**
 * The one source of truth for text-control styling. Field owns label/error
 * wiring; this owns the visual treatment — previously copy-pasted as a local
 * INPUT_CLASS constant in seven files.
 */
export const INPUT_CLASS =
  'w-full rounded-control border border-line bg-surface px-3 py-2 text-base text-ink';
